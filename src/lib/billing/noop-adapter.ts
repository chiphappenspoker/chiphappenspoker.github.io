/**
 * Placeholder until RevenueCat / Stripe adapters exist.
 * Real implementation will refresh `profiles.pro_unlocked_at` from the billing provider.
 */
export async function syncEntitlementsFromBilling(): Promise<void> {
  return;
}
