/** Max cloud sessions a FREE user may own (`created_by`); 11th insert fails at RLS. */
export const FREE_TIER_OWNED_SESSION_CAP = 10;

/** Max sessions shown in History for FREE (merged list, newest first). */
export const FREE_TIER_HISTORY_DISPLAY_CAP = 10;
